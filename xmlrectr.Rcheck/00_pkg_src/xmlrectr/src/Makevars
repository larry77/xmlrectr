# xmlrectr links directly to libxml2. pkg-config keeps include/library paths
# portable across normal Unix-like installations. Backtick command
# substitution is portable across the make implementations supported by R.
PKG_CPPFLAGS = `pkg-config --cflags libxml-2.0`
PKG_LIBS = `pkg-config --libs libxml-2.0`
